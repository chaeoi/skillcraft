# Skillcraft 协作准则

本项目是面向 Codex 的个人技能工坊。协作代理在本仓库中工作时需严格遵循以下规范：

## 核心基调：坚决杜绝 AI 感、机械感与需求复述

在任何工程场景下（包括代码实现、前端界面、代码注释、PPT 文稿、文档撰写及对话交付）：

1. **严禁机械复述用户需求**：
   - 绝不把用户的 Prompt、指令或输入材料长句原封不动复制为前端副标题、页面占位符、代码注释或汇报标题；
   - 对话交互中绝不鹦鹉学舌（例如“好的，根据您的要求，我将为您...”）。
2. **坚决剔除假大空 AI 八股**：
   - 杜绝“赋能”、“全方位协同”、“一站式智能化平台”、“在这个数字化时代”等虚浮词藻与机械翻译腔；
   - 始终使用具体、清晰、严谨、自然的工程事实与学术语言。
3. **注释与文本求真去冗**：
   - 代码注释只解释非显而易见的底层时序、协议约束或边界不变量，绝不写“复述代码在做什么”的废话注释；
   - 任何生成物中绝不残留“由 AI 生成”、“根据需求编写”等任何元信息或制作痕迹。

## 命名与目录规范

- **直观单单词命名**：技能与目录名称统一采用直观单单词（如 `ppt`、`code`、`word`）或无连字符连续单词（如 `styleguide`），严禁使用 `-` 连字符。
- **目录即技能**：`skills/` 下的子目录名称必须与该目录下 `SKILL.md` 中 frontmatter 的 `name` 字段完全一致。

## 技能结构要求

每个独立技能均需符合 Codex 技能标准：

1. `SKILL.md`：必须存在且包含合法的 YAML frontmatter（包含 `name`、`description`）。
2. `agents/openai.yaml`：定义 Codex 界面展示名、品牌色及默认提示词（如 `$ppt`、`$code`）。
3. `references/`：详细规范和深入文档放入此目录，供按需读取，保持 `SKILL.md` 入口精炼。
4. `README.md`：每个技能需附带清晰的中文说明与安装示例。

## 验证与提交流程

- 修改或新增任何技能后，必须运行 `./scripts/validate.sh` 进行静态验证。
- 遵循最小改动原则，不随意变更未涉及的文件与格式。
